const Character = require('../characters-classes/character-class');

// Criação do objeto do chefão "O "Pequeno"" usado na primeira fase
const theLittleOne = new Character('O "Pequeno"', 115, 40, 5);

module.exports = theLittleOne;

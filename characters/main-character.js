const MainCharacter = require("../characters-classes/main-character-class");

// Criação do objeto do personagem principal usado durante o jogo inteiro
const mainChar = new MainCharacter('Desconhecido', 100, 25, 0)

module.exports = mainChar
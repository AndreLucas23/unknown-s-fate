const Character = require('../characters-classes/character-class');

// Criação do objeto do inimigo "Criatura Real" usado na segunda fase
const royalCreature = new Character('Criatura Real', 180, 50, 10);

module.exports = royalCreature;
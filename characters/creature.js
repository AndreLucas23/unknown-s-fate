const Character = require('../characters-classes/character-class')

// Criação do objeto do inimigo "Criatura" usado na segunda fase
const creature = new Character('Criatura', 220, 60, 0);

module.exports = creature;
